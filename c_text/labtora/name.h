// name.h -- 自己编写的头文件，用于定义结构、变量，还用于引用结构。

#ifndef _NAME_H_
#define _NAME_H_
// 这是标准头文件名。

int per_cent(int a, int b);
char *s_gets(char *st, int n);

#endif
    // static 静态本地变量。
    // extern 声明一个变量，就是定义一个全局变量。但是这个变量的定义要去函数库内定义。
    // typedef 定义一个结构，用来定义这个结构的名字。